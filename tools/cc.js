// Generated by tools/gen-cc.mjs from shared/aurora_protocol.h. Do not edit.
//
// Run `node tools/gen-cc.mjs` after changing the header; `--check` fails if
// this file is stale.

(function (global) {
  'use strict';

  const CC = {
    tempoDivision:  2,
    faderColor:     12,
    faderExtent:    13,
    faderMotion:    14,
    padX:           15,
    padY:           16,
    padPressure:    17,
    padEngage:      18,
    rockerPadA:     19,
    rockerPadB:     20,
    rockerPadC:     21,
    rockerPadD:     22,
    rockerFaders:   23,
    audioFollower:  24,
    audioThreshold: 25,
    keyHeld:        26,
    washLevel:      33,
    washHueOffset:  34,
    washSaturation: 35,
    hue:            38,
    saturation:     39,
    value:          40,
    colorRegion:    41,
    colorRuler:     42,
    placedHue:      43,
    placedWhite:    44,
    placedDark:     45,
    placedCount:    46,
    placedWidth:    47,
    placedEdge:     48,
    placedSpeed:    49,
    wanderHue:      50,
    wanderWhite:    51,
    wanderDark:     52,
    wanderRate:     53,
    wanderScale:    54,
    litHue:         55,
    litWhite:       56,
    litDark:        57,
    genAlternate:   60,
    genBounce:      61,
    genWidth:       62,
    genCount:       63,
    genEdge:        64,
    genTail:        65,
    genPosition:    66,
    genSpeed:       67,
    genFanFreq:     68,
    genFanPhase:    69,
    genFanRandom:   70,
    genFan:         71,
    genFanRate:     72,
    genFanPulse:    73,
    genPulseRate:   75,
    scatterRate:    83,
    scatterCount:   84,
    scatterWidth:   85,
    scatterEdge:    86,
    scatterStagger: 87,
    scatterDrift:   88,
    scatterLight:   89,
    scatterHue:     90,
    scatterWhite:   91,
  };

  const ROUTES = 8;
  const ROUTE_BASE = [76, 92, 96, 100, 104, 108, 112, 116];
  const ROUTE_FIELD = {"destination":0,"amount":1,"ratio":2,"wave":3};
  const ROUTE_MAX_RATIO = 8;

  const routeCC = (route, field) => ROUTE_BASE[route] + field;

  // Whole multiples only, 1 through 8. Dividing would break the anchor.
  const routeRatio = v =>
    1 + Math.min(ROUTE_MAX_RATIO - 1,
                 Math.floor((v * (ROUTE_MAX_RATIO - 1) + 63) / 127));

  const GEN_WAVE_SWELL = 32;
  const GEN_WAVE_SAW_DOWN = 64;
  const GEN_WAVE_SQUARE = 96;
  const GEN_PULSE_MIN_WIDTH = 0.06;

  const NAME_BY_CC = {};
  for (const [name, number] of Object.entries(CC)) NAME_BY_CC[number] = name;

  global.AuroraCC = {
    CC, NAME_BY_CC, ROUTES, ROUTE_BASE, ROUTE_FIELD, ROUTE_MAX_RATIO,
    routeCC, routeRatio,
    GEN_WAVE_SWELL, GEN_WAVE_SAW_DOWN, GEN_WAVE_SQUARE, GEN_PULSE_MIN_WIDTH,
  };
})(typeof window === 'undefined' ? globalThis : window);
